# echannel

1.安装方法：

a)mvn clean install -Dmaven.test.skip  -Denv=release

b)工程依赖：

<dependency>
 <groupId>org.codeanywhere.framework</groupId>
 <artifactId>echannel</artifactId>
 <version>1.0.0</version>
</dependency>

2.使用方法：

ChannelServerTest
